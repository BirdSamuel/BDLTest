name: bdl_test
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  bloc: ^8.1.3
  cupertino_icons: ^1.0.6
  flutter_bloc: ^8.1.4
  flutter_svg: ^2.0.10+1
  google_fonts: ^6.2.1
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/PanthersPrimaryLogo.png
    - assets/PanthersTertiaryLogo.png